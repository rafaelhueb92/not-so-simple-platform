apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: hello-route
  namespace: eks-gateway-demo
spec:
  parentRefs:
    - name: hello-gateway
      sectionName: https
  hostnames:
    - hello.api.com
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: hello-service
          port: 80
